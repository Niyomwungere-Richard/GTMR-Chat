import { type SVGProps } from "react";
import { MessageCircle } from "lucide-react";

export function GtmrChatLogo(props: SVGProps<SVGSVGElement>) {
  return (
    <MessageCircle {...props} />
  );
}
